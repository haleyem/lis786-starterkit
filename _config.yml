title: Open Access Feminist Resources
theme: minima
collections:
  theories:
    output: true
  vocab:
    output: true


defaults:
  - scope:
      type: "theories"
    values:
      layout: theory_item

defaults:
  - scope:
      type: "vocab"
    values:
      layout: vocab_item